<%= form_with(model: titan_role, local: true) do |form| %>
  <% if titan_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(titan_role.errors.count, "error") %> prohibited this titan_role from being saved:</h2>

      <ul>
        <% titan_role.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titan_id %>
    <%= form.text_field :titan_id %>
  </div>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.text_field :role_id %>
  </div>

  <div class="field">
    <%= form.label :started_at %>
    <%= form.date_select :started_at %>
  </div>

  <div class="field">
    <%= form.label :ended_at %>
    <%= form.date_select :ended_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
