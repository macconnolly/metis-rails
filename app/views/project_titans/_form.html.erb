<%= form_with(model: project_titan, local: true) do |form| %>
  <% if project_titan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_titan.errors.count, "error") %> prohibited this project_titan from being saved:</h2>

      <ul>
        <% project_titan.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.text_field :project_id %>
  </div>

  <div class="field">
    <%= form.label :titan_id %>
    <%= form.text_field :titan_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :comments %>
    <%= form.text_field :comments %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
